import {CandleInterval} from "tinkoff-invest-api/cjs/generated/marketdata";
import {get_price_change} from "./common";

//const tickers_10_00_main_session: string[] = require('../ms-crawler/spbe.10_00_main_session.tickers.json');
//get_price_change('SPB', tickers_10_00_main_session, CandleInterval.CANDLE_INTERVAL_DAY)
//    .then(() => 'ms get-price-change runned for tickers_10_00_main_session');


//const tickers_10_00_main_session: string[] = ["NVDA", 'AMD']

//const rus_tickers:string [] = require('../ms-crawler/spbe.10_00_main_session.tickers.json');
//get_price_change('MOEX', rus_tickers, CandleInterval.CANDLE_INTERVAL_DAY);

//console.log(['AMD', 'TSN'].join(','))

//tickers.join(', ')